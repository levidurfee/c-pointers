#include <stdio.h>
#include "pointers.h"

int main() {
    if(indirection()) {
        printf("Completed 1\n");
    }
    if(movies()) {
        printf("Completed 2\n");
    }
    if(lol_constants()) {
        printf("Completed 3\n");
    }
    if(dma()) {
        printf("Completed 4\n");
    }
    return 0;
}